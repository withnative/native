# Schema admission fixtures

These fixed-time examples pin canonical v1 request, observation, refusal, and
plan shape and its exact fleet digest. The two refusal fixtures cover both an
unavailable observation and an exact-host binding mismatch. They contain no
trusted provenance and authorize nothing.
