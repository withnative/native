[package]
name = "native-relationship-kernel"
version = "0.0.0"
edition = "2021"
rust-version = "1.98"
description = "Storage-free governed relationship reduction for native-ce."
repository = "https://github.com/withnative/native"
license = "AGPL-3.0-only"
publish = false

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
